# Public Repository Metrics Example

Command:

```bash
python -m maintainer_signal_kit github ismailskr12/maintainer-signal-kit
```

This command uses GitHub's public repository API and does not require a token.
It is meant to support evidence collection, not to inflate adoption claims.
